name: release

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

concurrency:
  group: release-${{ github.head_ref || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  changes:
    runs-on: ubuntu-latest
    outputs:
      check_changeset: ${{ steps.detect.outputs.check_changeset }}
      release: ${{ steps.detect.outputs.release }}
    steps:
      - uses: actions/checkout@v6
        name: checkout
        with:
          fetch-depth: 0
      - id: detect
        name: detect
        run: |
          changed_files="$(mktemp)"

          if [ "${{ github.event_name }}" = "pull_request" ]; then
            base="${{ github.event.pull_request.base.sha }}"
            head="${{ github.event.pull_request.head.sha }}"
          else
            base="${{ github.event.before }}"
            head="${{ github.sha }}"
          fi

          if [ -z "$base" ] || [ -z "${base//0/}" ]; then
            git diff-tree --no-commit-id --name-only -r "$head" > "$changed_files"
          else
            git diff --name-only "$base" "$head" > "$changed_files"
          fi

          check_changeset=false
          release=false

          while IFS= read -r file; do
            case "$file" in
              src/*|package.json|bun.lock|README.md|CHANGELOG.md|justfile|.changeset/*.md|scripts/check-release-changeset.ts|.github/workflows/*)
                check_changeset=true
                ;;
            esac

            case "$file" in
              .changeset/*.md|package.json|bun.lock|CHANGELOG.md|.github/workflows/release.yml)
                release=true
                ;;
            esac
          done < "$changed_files"

          {
            echo "check_changeset=$check_changeset"
            echo "release=$release"
          } >> "$GITHUB_OUTPUT"

          echo "Changed files:"
          sed 's/^/- /' "$changed_files"

  changeset:
    if: ${{ github.event_name == 'pull_request' && needs.changes.outputs.check_changeset == 'true' }}
    needs: changes
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        name: checkout
        with:
          fetch-depth: 0
      - uses: jdx/mise-action@v2
        name: setup[mise]
        with:
          install_args: bun
      - name: check
        run: bun scripts/check-release-changeset.ts

  release:
    if: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' && needs.changes.outputs.release == 'true' }}
    needs: changes
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write
    steps:
      - uses: actions/checkout@v6
        name: checkout
        with:
          fetch-depth: 0
      - uses: jdx/mise-action@v2
        name: setup[mise]
        with:
          install_args: bun just
      - name: setup[npm]
        run: |
          mkdir -p "$RUNNER_TEMP/npm-wrapper"
          cat > "$RUNNER_TEMP/npm-wrapper/npm" <<'SCRIPT'
          #!/usr/bin/env bash
          exec bunx --bun npm@latest "$@"
          SCRIPT
          chmod +x "$RUNNER_TEMP/npm-wrapper/npm"
          echo "$RUNNER_TEMP/npm-wrapper" >> "$GITHUB_PATH"
          npm --version
      - name: deps
        run: bun install --frozen-lockfile
      - name: deps[vscode]
        run: cd ext/vscode && bun install --frozen-lockfile
      - name: check
        run: just check
      - name: test
        run: just test
      - name: version
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          if ! compgen -G ".changeset/*.md" > /dev/null; then
            echo "No changesets present; package is already versioned."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          bun changeset version
          bun install --lockfile-only
          git add -A

          if git diff --cached --quiet; then
            echo "changeset version produced no changes; nothing to commit."
            exit 0
          fi

          git commit -m "chore(release): bump skill-language-server version"
          git push origin "HEAD:${GITHUB_REF_NAME}"
      - id: publish
        name: publish
        run: |
          version="$(jq -r '.version' package.json)"
          echo "version=$version" >> "$GITHUB_OUTPUT"

          if npm view "skill-language-server@$version" version >/dev/null 2>&1; then
            echo "skill-language-server@$version is already published; skipping."
            echo "published=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          just build-server
          npm pack --dry-run --json
          bun changeset publish
          release_version="skill-language-server@$version"
          if ! git rev-parse "$release_version" >/dev/null 2>&1; then
            git tag "$release_version"
          fi
          echo "published=true" >> "$GITHUB_OUTPUT"
          git push origin --tags || echo "::warning::Failed to push git tags for the published release."
